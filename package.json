{
  "name": "arena-one",
  "private": true,
  "version": "1.0.0",
  "description": "Arena One — Agent Gaming. Watch the match. Take a seat.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
