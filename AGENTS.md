# Agent Gaming

Harness canon lives in `github.com/timlinnet/harness`. Overlay: `github.com/timlinnet/harness-private`. This repo holds pointers only. Do not bake, vendor, or clone the canon here.

## Boot — every coding agent (Cursor Cloud, Grok Build)

1. Fetch `PRINCIPLES_INDEX.md` from `github.com/timlinnet/harness` (`gh api` or `git`). Do not clone the repo into this tree.
2. Load full entries only when they bear on the live change.
3. Before any public URL / checkout / brand change, run three cuts:
   - **leftover** — delete it
   - **compose** — Arena One is the product; Stripe is the card rail
   - **substrate** — Agent Gaming mark, not FreedomOS / getfreedomos.com chrome
